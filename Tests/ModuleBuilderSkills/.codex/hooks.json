{
  "hooks": {
    "SessionStart": [
      { "hooks": [ { "type": "command", "command": "dotnet run .codex/hooks/gate/gate.cs -- warm" } ] }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|ApplyPatch",
        "hooks": [ { "type": "command", "command": "dotnet run .codex/hooks/gate/gate.cs --no-build -- guard-write --harness codex; test $? -eq 0 && exit 0 || exit 2" } ]
      },
      {
        "matcher": ".*run_designer_script.*",
        "hooks": [ { "type": "command", "command": "dotnet run .codex/hooks/gate/gate.cs --no-build -- guard-version --harness codex; test $? -eq 0 && exit 0 || exit 2" } ]
      }
    ],
    "Stop": [
      { "hooks": [ { "type": "command", "command": "dotnet run .codex/hooks/gate/gate.cs --no-build -- close-out --harness codex; test $? -eq 0 && exit 0 || exit 2" } ] }
    ]
  }
}